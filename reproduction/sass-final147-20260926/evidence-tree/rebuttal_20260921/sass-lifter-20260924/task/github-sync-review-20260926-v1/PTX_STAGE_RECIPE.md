# Final147 PTX stage provenance

Read-only, no transforms/build/GPU. Machine-readable exact commands, hashes, source dependencies and small-file list: PTX_STAGE_RECIPE.json.

Li6 actual historical HBF transform command is preserved in task/qkv-hbf-stage-v1/run_stage.sh. run-v1/source.sha256 ties source6db074..., pass0ab365..., and prepare_triton_ptx.py f385d3...; it runs Python --transform-one --pass-library <frozen pass> --kernel <exact Li6 symbol>, reads recovered quarantine PTX from stdin and writes staged PTX/stdout with HBFSIM_PASS_MANIFEST_PATH set. Output7219c1... and manifest7e6efb... are registered in qkv-hbf-replay-v1/stage-v1/STAGE_RECEIPT.json. prepare_stage.py then gives transformed content the ORIGINAL source hash filename.

Li7 recovered sourcee70c7c... is generated by same frozen v7 Rust lifter from sliced official SASS; run_li7_cpu_v2.py and run-v2/CPU_RECEIPT.json preserve actual recovery, PTXAS and metadata argv. prepare_first_image_v2.py removes noisy empty fatbin wrappers with input/slice identities. host-adapter-v1 PAUSE_CHECKPOINT records same pass0ab365..., staged7ac0e8..., manifest21a18f..., rc0 and243 rewrites/0 unsupported. A verbatim Li7 transform argv was NOT found in inspected receipts. The identical generic Python command with exact Li7 symbol/input is a source-derived reconstruction recipe, NOT a recovered historical shell transcript. This is sufficient to specify a future transformation; do not relabel it observed or CPU-revalidated.

The pass imports helper PTX generated by first-fault-compact-source-v2, not later B-weight snapshots. Actual build.ninja helper command is copied into JSON: CUDA13.0 nvcc --ptx --std=c++20 --gpu-architecture=compute_120 --relocatable-device-code=true --keep-device-functions, compiler g++15, TIMING_FUTURES=1 and both EVAL diagnostic flags0, followed by cmake/EmbedDevicePtx.cmake. Helper297388B/f9efe085... is recorded as exact substring in Li6 staged PTX. libptxpass_hbf target compiles plugin.cpp and links frozen core/future-emitter/PTX-IR/MQSim archives plus crypto/CUDA/rt; rebuild those from the pinned source+patch closure, not copied binaries.

Public completeness requires the small files listed in JSON plus source closure already being assembled by packet owner. Li6 reproducible Rust entry and both licenses are in qkv-output-source-delivery-v1; use its profile/receipt rather than inventing historical missing Cargo argv. Vendor input extraction scripts and acquisition identities can replace redistribution of CUBIN/SASS. HBF stage transformation uses recovered PTX directly; standalone assembled CUBIN is validation evidence, not its input. Final prepared-v2 joins source-hash-named stage files, pass manifest and sidecars; regenerate path references consistently and never overwrite historical receipt identities.
