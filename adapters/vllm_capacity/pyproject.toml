[project]
name = "hbfsim-vllm-capacity"
version = "0.1.0"
description = "Auditable Qwen routed-expert tracing, replay, and capacity staging"
requires-python = ">=3.11"
dependencies = []

[project.entry-points."vllm.general_plugins"]
hbfsim_capacity = "adapters.vllm_capacity.capacity_plugin:register"

[tool.ruff]
line-length = 88
