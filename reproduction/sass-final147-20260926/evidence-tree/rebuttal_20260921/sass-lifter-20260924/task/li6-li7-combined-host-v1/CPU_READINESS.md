# Combined Li6/Li7 host CPU packet

Status: **CPU_BUILD_PASS, CPU fixtures PASS, GPU/model NOT DEPLOYED**. Root remains sole GPU owner; epoch6706 files were not read or edited. This candidate is isolated from the frozen Li6/Li7 runtime packages. It is default-off. The current expansion gate requires an actual head representative `MODEL_CONNECTED_PASS` before any combined GPU validation.

The changed units are `source/nv_attach_impl_frida_setup.cpp`, `source/provider_router_exact.cpp`, and `source/qkv_live_identity_v1.hpp`. The agent setup unit alone replaces its same-named member in the copied Li7 `build-agent-v2/libbpftime_nv_attach_impl.a`; the unchanged `nv_attach_impl.cpp.o` and other archive members remain. `build-v5/agent_setup_compile.argv.json` → `nv_attach_impl_frida_setup.cpp.o` → `agent_setup_archive.argv.json`/`agent_ranlib.argv.json` → `libbpftime_nv_attach_impl.a` → `agent_link.argv.json` → `libbpftime-agent.so`. Provider `provider_compile.argv.json` compiles the changed provider plus unchanged `library_identity_core.cpp` directly to `libprovider_router.so` (archive N/A). The unchanged gate is Li7 `build-gate-v2/libhbfsim_launch_gate.so`. Full product/source identities and dynamic export/resolution records are in `CPU_BUILD_RECEIPT.json`, `CPU_SOURCE_IDENTITY.json`, and `build-v5/`. Build-v1 and build-v2 failed only because isolated copies initially lacked local headers and companion source; their stderr/receipts are preserved. Build-v3 and build-v4 were intermediate successes before final source changes; build-v5 is the current final product.

The provider CPU fixture proves typed Li6 and Li7 queries can interleave in the same admitted image; wrong entry, wrong context, stale module, wrong image, and invalid entry refuse, while old V1 query retains its single-profile behavior. It also calls the actual callback-field formatter for both exact symbols and rejects a third symbol. The config fixture compiles the **exact source block** and proves a complete two-row config, missing other row, wrong other pin, conflicting legacy target kind, default-off, and immutable value after environment mutation. Neither fixture loads a CUDA context; the provider constructor has no correlation-log/module-dir configuration in that process, so it returns before CUPTI setup. `SELECT_END_REUSE.json` verifies the V1 select/end function bodies are unchanged from the Li7 source, preserving its existing duplicate/missing-selected failure semantics. These CPU checks cannot prove a live mixed model binding or service completion.

Source-backed reuse: the unchanged agent binder (`router-li7-host-adapter-v1/source/nv_attach_impl_router_scoped.cpp:804-928`) keys patched variants by **PTX digest plus exact kernel name**, stores original→patched in a map, rejects a conflicting rebind, and leaves old98 name fallback available while disabling name-only fallback for the two exact entries. The combined agent has one bound original/patched/context/module/token per exact profile and retains the real 152-byte aggregate→19-cell adapter. Li6's three existing envelopes (QKV 25,165,824B/grid1536, o_proj 8,388,608B/grid512, head 206,045,184B/grid12576) are admitted only in combined mode; legacy target knob behavior remains unchanged when off. Li7 remains router 262,144B/grid16. Separate source/staged paths and map/stage pins are required at process configuration; the loader's actual two hash-named staged-file sidecar/manifest package and plugin request joins are **future packaging work**, not present GPU evidence.

The final combined acceptance plan remains `task/full-coverage-ledger-v1/EXPANSION_AND_FINAL_ACCEPTANCE_v1.md`: short changed-component model interleave only after head representative pass, then remaining storage expansion and one final147 union regression. Current CPU evidence does not claim that the earlier two-selector head timeout is fixed. No new per-storage counter ABI or PTX/helper change was made. Per-storage acceptance uses addressed modeled rows, selected actual base/extent/output/dispatch links, and final exact-module closure as in accepted epoch6603; shared-module counts are not separate per-storage latency measurements.
