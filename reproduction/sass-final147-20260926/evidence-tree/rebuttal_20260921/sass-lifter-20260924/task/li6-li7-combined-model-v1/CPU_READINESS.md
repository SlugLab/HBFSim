# Combined model adapter: CPU package readiness

Status: **CPU_READY_FOR_ROOT_REVIEW; NO GPU OR MODEL PASS**. The isolated package is default-off (`HBFSIM_COMBINED_MODEL_PLUGIN_V1=1` is required). It does not modify frozen QKV, o_proj, router, head, gate, PTX, or helper artifacts. `prepared-v1` is preserved as the superseded scope draft; `prepared-v2` is the corrected package.

The package now includes `combined_model_adapter-0.1.dist-info/entry_points.txt` for `vllm.general_plugins`; `importlib.metadata` discovered and loaded its inert default-off entry point in the CPU fixture. `bundle-combined-v1` links the unchanged base runtime to the combined build-v5 agent/provider and frozen Li7 gate; it copies no large binaries. `BUNDLE_JOIN.json` records configured→resolved device/inode joins.

`prepare_cpu_package.py` creates three immutable scope manifests: layer-0 QKV/o_proj/router (3), layers 1–15 of those categories (45), and all 48 plus `lm_head` (49). The final loader registration contract remains 147 storages, comprising the existing 98 and these 49; the plugin-selected count is not itself full coverage. Each row matches the recorded full BF16 storage, output, exact independent Li6 or Li7 symbol and image, Driver API/CBID/geometry, and BLAS dimensions, types, transposition, strides, algorithm, operand and offset from `full-coverage-ledger-v1/NATIVE_PROFILE_REUSE.json`. The stage package has nine source-SHA-named symlinks, the existing old-98 and Li6 pass rows plus the Li7 pass row, and relocated sidecar/native-binding joins. Recorded content hashes for unchanged large assets are reused; `STAGE_JOIN_RECEIPT.json` records the package paths and manifest identities. Absolute stage/sidecar paths are generated from this package location at preparation time, not embedded as research logic or machine identity requirements.

`combined_model_adapter/__init__.py` imports inertly until explicit opt-in and rejects concurrent old-plugin activation. It installs one loader/runner wrapper, validates each target's actual full storage, and preserves the original callable and tuple/bias behavior. On the one scheduled decode request it saves a cloned-activation native reference before select, then records select, candidate and end (in `finally`), independent output bytes, exact output equality, pointer/extent/profile/order, request ID, OS thread, stream and timestamps. Head selection occurs after the forward path in `compute_logits`; `execute_model` must return with every selected target complete. Non-target consumers remain outside the selected claim. Actual CUDA API/CBID and provider identity must be joined from runtime provider/agent receipts, never inferred from this Python adapter.

`CPU_CONTRACT_RESULT.json` records the passing narrow CPU fixtures: inert import without torch/vLLM, manifest counts and duplicate rejection, native-before-select/candidate/end ordering, candidate failure preservation with end, simultaneous candidate/end exception preservation with an end-failure note, repeated child rejection, head thread mismatch, missing forward child, post-forward head completion, missing head, and request mismatch. The manifest preparation completed once as `prepared-v2`; this evidence is host-contract verification only. The fake runner does not prove actual vLLM dispatch, loaded provider paths, GPU output, service, or final 147 coverage.

Before any launch, root must review this CPU package, observe a terminal head representative `MODEL_CONNECTED_PASS`, and prepare a separate finite preflight and launch plan. Select the GPU from configured logical device/inventory, check occupancy on that selected device, and record observed UUID/host without historical identity equality gates. No live plan or GPU start is included here. The required execution order remains mixed 3 pilot, remaining 45 expansion, then one final 147 union regression using the final component; a prior standalone head lift is not model coverage.

`validate_combined_model.py` is a strict read-only evidence validator for mixed3/remaining45/added49 (registrations 3/45/147). It requires run/owner/controller plan identity, actual configured→resolved mapped DSO path/device/inode, target environment/device occupancy, independent raw activation/native/candidate bytes, one selected plugin/request/thread/order/base/extent row per target, one agent decision and unique patched provider callback per selected call, exact original provider image proof, per-storage modeled address rows, and positive exact-module service/byte closure with zero errors. For final147 it reuses the accepted old98 address/active-module criterion with the seven old modules, excluding both Li6 and Li7. `VALIDATOR_SYNTHETIC_RESULT.json` records passing synthetic negative cases only; no fabricated row is treated as runtime evidence.

`MIXED3_PLAN_TEMPLATE.json` and `root_mixed3_launcher.py` are CPU-only future launch preparation. There is **no `MIXED3_LIVE_PLAN.json`**; prepare refuses without a real head model PASS and fresh finite resource receipts. The template's proposed budget is worker1800/outer2100/collection600/startup300/full3000 seconds. The 480-second request timer remains the successful representative's per-request service-wait bound; it does not bound aggregate candidate wall time. The earlier 1500-second draft is preserved as superseded. `run_bounded_selected_gpu_stage.py` and `resource_guard_selected_gpu.py` are isolated copies that accept a configured GPU index, query only that GPU for resource/occupancy checks, log its observed UUID, and reject this worker group if it appears on another GPU. The finite PID/start/group/deadline, 5-second guard sample, and two-bad-sample termination controls are retained. `SELECTED_GPU_CPU_RESULT.json` covers only mocked commands/config and no actual process, signal, nvidia-smi, or GPU execution. The launch path requires a separate root review receipt and host-level preflight; no historical GPU UUID is a fixed research gate.
