# Resume contract — 2026-09-07

Execution request: `results/batches/20260907-formal-matrix-resume/execution-request.txt`.
Worktree: `/root/hbfsim-exp/eval-base-integration`, branch
`eval/eq1-eq4-implementation`, starting HEAD
`aa5af1d125bbd5c758a0393a099e51e17e7fff51`. Three preexisting documentation
changes are preserved in the batch's `preexisting.patch`; untracked results and
figures are retained. No reset, cleanup, system changes or private paper edits.

## Sources and history boundary

Read the current 49 plan, original matrix, hardware/claim/schema/figure/execution
contracts; current 50 status, blockers, budgets and integration descriptions;
the preceding routing batch's receipts; scheduler and affected implementation.
The two original histories were read in this task's prior turn. Their known
empty recent items remain unread, rather than reclassified as recovered history.
The backup task `01a07b84-1d9e-72d2-b14a-367657fd4afd` was read once; its latest
turn also returned no items. Its actual stream script and host process supplied
current evidence. No unrelated session or credential scan was performed.

PID 2916359 was pinned by its process handle and observed exiting. The server
stream completion receipt is retained in `server-transfer.sha256`, digest
`31b13d225b0dcd3188dba4c090c2e25a6ab560d39de02d9e9d51bc89110839da`.
This confirms server stream completion, not the Windows checksum comparison.
Project edits and experiments begin only after this receipt. Old data is retained.

## Frozen objective and reusable evidence

Original matrix SHA256:
`33de7cad2731d611a92a4be0852aa98b914f62e227a720d3e3e0eb007be8462e`.
The existing `load_matrix`/`select_runs` expand 2,848 unique conditions into
20,485 unique runs; minimum is 1,477 conditions / 13,105 runs. Resource totals:
CPU_ONLY 10,210; GPU_EXCLUSIVE 6,635; GPU_SHARED_SAFE 120;
GPU_STORAGE_EXCLUSIVE 960; STORAGE_EXCLUSIVE 2,560.
Complete minimum through applicable gates, then only the separately authorized
extension. Do not silently reduce the full denominator or launch ideal now.

Reuse the five fixed-token captures and B1–16 composition arithmetic only within
their recorded repeatability/PROJECTED scope. HF012 is not compute-only time.
P7 prefix-media receipts are not common-profile end-to-end performance. C6 D20
expired before work; G2's one-block two-pair observation does not close broad G2.
Formal DONE is initially zero; acquisition, validity and claim outcome stay separate.

## Immediate implementation sequence

- [ ] Add a coverage export using existing matrix expansion and scheduler state;
  preserve every original repeat, absent handler/gate, and raw/validator reference.
- [ ] Establish the smallest scientifically valid formal adapter. Routing gates
  are G1/G8/G9 only; its scheduler/inner GPU lock conflict needs authenticated
  ownership reuse, never an environment-only bypass. Formal natural inputs need
  a frozen amendment: original dataset, lengths/seeds and the 16-prompt/B32–128
  mismatch are unspecified. The user question is pending; no values are invented.
- [ ] Extend the existing G2 same-process ABBA diagnostic to exactly two blocks.
  Hypothesis: concurrent chains introduce the non-wait disturbance. Keep K1/W1,
  D0/D500, allocation/module, all rows and original 100/200 ns diagnostic limits.
  If two blocks remain accurate, concurrency alone is insufficient. Do not retry
  the old failing geometry without a new discriminator. This does not itself
  complete original gpu_delay-00001..00003/00031..00033.
- [ ] Repair C6 observer preparation within the diagnostic helper, keeping the
  true issue anchor, intrinsic checks, ready formula and full helper-entry timing.
  First preserve malformed/header/range failure behavior, then inspect optimized
  image and spills. One matched original-D20 acquisition distinguishes observer
  contamination from remaining intrinsic overhead. It is not G5 certification.
- [ ] Verify the original 1 TiB MQSim geometry under a finite revised process
  budget. Its 67,108,864 pages require at least 2 GiB page/GMT metadata before
  other allocations, so old 2 GiB AS is impossible. Start with a justified bounded
  4 GiB AS initialization/small replay, host memory/commit reserves and no geometry
  substitution. Physical storage remains externally blocked without an authorized
  dedicated file on a truly exclusive backing device.

Use scoped failing tests, minimal repairs, corresponding independent reviews,
then frozen-source pilots. Build/edit stages precede device batches: never change
their loaded source/configuration during execution. GPU work is serial/exclusive;
CPU concurrency requires explicit RAM/commit/disk budgets. Waiting uses process
exit notification, with low-frequency user-level progress checks.

## Initial resource observation

Host gpu01, RTX PRO 6000 Blackwell, UUID
`GPU-f07ea2df-1b6f-9a02-b534-5090abf3c174`, driver 595.84, 97,887 MiB total.
GPU empty at preflight. MemAvailable 19,632,524 KiB; CommitLimit/Committed_AS
70,960,824/51,357,180 KiB. These are observations, not reservations.
Root is shared Lexar `/dev/nvme1n1p2`; last host free bytes 10,971,521,024.
`/mnt/disk0` contains the backup, not an authorized experiment output target.
Unmounted CD8P devices are not implicit permission for payload I/O or mounting.
