# Final evidence report recipe — CPU preparation only

The final run is active; this generator has **not** been run against it. No active output or live status was read. `report_final.py` requires existing controller terminal evidence and saved frozen-validator `MODEL_CONNECTED_PASS_COMBINED_ADDED49` before it reads the remainder of the run. It does not launch work, call CUDA, alter acceptance criteria, or rerun validation. Syntax was checked; only saved remaining45 schema was inspected during preparation.

After root has terminally collected and validated final147, run the generator with `--run` pointing to that exact run, `--target-ledger` to `task/full-coverage-ledger-v1/TARGET_LEDGER.json`, `--historical-union` to `task/full-coverage-ledger-v1/MODEL_COVERAGE_AFTER_REMAINING45_v1.json`, and `--output-dir` to a new report directory. Use absolute configured paths. Output consists of a short REPORT.md and a machine-readable REPORT.json; originals are read-only and existing output paths are refused.

The report retains all147 live registered bases/extents and accepted module relationships, streams modeled coverage parameter-address rows by accepted module/range, copies exact module/aggregate access-admission-completion counters, and lists49 independent selected output receipts plus final tokens. Parameter addresses are not an exhaustive trace of element addresses; registered extent is not unique bytes touched. Service requests are not interchangeable with sampled reference completions. Shared module counts are not per-storage latency/count attribution. Exact experts/lanes triggered, all unsupported consumers, and native prefill coverage are marked UNKNOWN/NOT_MEASURED unless separately established; no all-consumer claim is inferred from registration or global positive counters. The historical147 union remains distinct from the actual final single-run result.

Completed-run schema review: remaining45 coverage contains observed nonmodeled rows, including `reason=allowed` with `modeled=false`/`range_policy=none`, and `reason=opaque_unmodeled_timing` with `operation=opaque_pointer_access`. Consumer names and some module IDs are present (for example rms_norm, Fill, fused_moe and elementwise functions); API/CBID are absent in this coverage schema and must remain null/unknown rather than inferred. The generator now streams and groups all nonmodeled rows by actual reason/module/kernel/API/CBID/operation/policy/flags. These are observed categories, not an exhaustive unsupported instruction inventory; allowed-but-unmodeled is not automatically unsupported. Phase receipts separately preserve scheduled prefill positions [0,1] and decode [2]. Canonical plugin source activates selected49 TLS scope only in decode and uses original forward in prefill; raw coverage has no direct phase/request join, so nonmodeled groups are not assigned to prefill. This schema review used only the completed remaining45 run, not active final147.
