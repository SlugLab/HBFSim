%-------------------------------------------------------------------------------
\section{Motivation}
\label{sec:motv}
%-------------------------------------------------------------------------------
% This is the section a FAST reviewer reads to decide whether the problem is
% real. It must carry measured evidence, not argument.
%
% Plan:
%   2.1  What breaks today: capacity and cost of the fast memory tier.
%   2.2  What a synthetic parameter sheet cannot tell you -- show the measured
%        curve against the analytic model here, this is the strongest single
%        piece of evidence we have.
%   2.3  Why temperature belongs in the model and not in a footnote:
%        sustained load changes the device's own behavior, so a timing model
%        calibrated at one thermal state does not hold at another.
%   2.4  Requirements list that the design sections must then satisfy.
\TODO{motivation}
