# 蓝队处置报告

先交代两个贯穿全篇的修复件,后面反复引用,编号 F1–F7:

- **F1(epsilon 零延迟臂)**:profile 校验拒绝的是 `read_latency_ns == 0`(核查报告 §3,`src/profile/profile.cpp:330` [一手]),不是拒绝小值。构造一份全参数取最小合法值的 epsilon profile(如 1 ns),先测它的残余建模时间;残余 ≤ 最小目标延迟(单页 11,133 ns)的 1%(约 111 ns)即接受为零延迟对照臂,不改一行代码。1 ns/11,133 ns = 0.009%,污染可忽略。残余超标才走一个旁路开关,与 item 7 同类,并入 F6 的一次性快照。代价:半天验证。
- **F2(后备介质可切换性已被回答)**:核查报告 §7 EQ2 行 [一手]:「backing 是调用方给的文件路径,机制上可放任意设备,但无选择 harness 与对照脚本」。即 C0 §5.2 / D1 E1.3 / D2 E1-c 的「前提未知」实际上是「机制已确认、缺脚本」。缺的是脚本,不是引擎。
- **F3(阻塞语义下的回放对照)**:冻结轨迹没有到达时间,但它声明的语义就是「ordered blocking demand misses with no compute gaps」(核查报告 §2 [一手])。在硬件上按同样的阻塞顺序回放(上一请求完成才发下一个),两侧语义严格匹配,不需要到达时间。这条对照验证的是服务时间与顺序行为,明写不覆盖开放到达的排队——排队由 F4 单独覆盖。
- **F4(QD 配对扫描)**:硬件侧 fio/io_uring 在 CD8P 裸设备上扫 QD 1–128;模拟侧 `hbf_mqsim_bench` 已有 `--bytes/--pattern/--arrival-gap-ns`(核查报告 §7 EQ1-B 行 [一手]),同到达过程喂 MQSim 引擎。这直接锚定 reference 引擎的排队行为——正是 PR#3 那类 bug 唯一的实验防线(红3 D2-4 自己给出的修法)。回放引擎恒 QD=1 的限制不适用于这条路径。代价约 1 天(脚本 + 机时)。
- **F5(跨引擎缝合实验)**:回放引擎本身支持 `--mode fast|hybrid|mqsim` 吃同一份未命中 JSONL(核查报告 §3 [一手])——「同一请求流喂两个引擎、报偏差」的基础设施已存在,只差一次运行。另加半天:同一请求流喂 MQSim 原生全栈配置与我们的截段适配器,报偏差(红1 盲区2 点名的「便宜而决定性的实验」)。
- **F6(快照策略)**:分析侧代码(held-out 切分、误差/排名计算、绘图)是离线脚本,不触碰运行时,不引发任何重跑级联——红2 盲区5 把这两类混在一起了。只有运行时改动(item 7 开关、F1 若需旁路)才要新快照;把它们全部合并成第 2 天的一次 Snapshot B,此后所有需要开关的运行都在 B 上跑,级联重跑收缩为零(需重跑的只有 PR#3 本来就判定失效的颗粒几何数字)。
- **F7(time_scale 第 0 天闸门)**:任何 E2/EQ2 图表与措辞之前,先在 time_scale=1 下复测 reference 基准点(半天)。两个分支都预先写好:差距保持,选档故事照写;差距坍缩,选档论据换轨到与 time_scale 无关的三个实测事实——37–51% 插桩地板 [一手]、profiling 同步请求风暴导致 61 GB 注册不可行 [一手]、reference 逐请求同步路径的覆盖-成本线性放大。故事换机制,不死。20.8x/44.469 两侧的原始 manifest 口径:缺,需合作者补。

---

## 一、红队 1(保真度方法学)

**C0-1(A/B 三条路全堵)——修复。** 三个堵点逐个拆:零延迟臂由 F1 解决(配置,非代码);后备可切换由 F2 驳倒其「未知」定性(核查报告已给答案);trace 回放退路由 F3 解决(阻塞语义匹配,不需要到达时间)。承认其中一半:开放到达的排队维度这条退路确实给不了,由 F4 补位。EQ1 不再悬空。代价合计约 2 天 + 一次 Snapshot B。

**C0-2(被验证的是哪个引擎没说)——修复。** 攻击成立,但修复便宜:F5 的缝合实验给出 fast/hybrid/mqsim 三档对同一真实未命中流的逐流偏差数字,论文里明写「CD8P held-out 误差属于实测曲线档;MQSim 档的资格由 F4 排队锚定 + F5 一致性偏差 + 原生全栈对照三件给出」。每个引擎有自己的锚,缝有实测偏差数。

**C0-3(门槛表无出处、自杀性、注册时点不可证)——一半驳倒一半修复。** 驳倒「自杀性」:门槛表是措辞政策不是及格线——宏观误差落在 FEMU 的 0.5–38% 量级(brief-2 §3)时,论文按表改用趋势措辞,这是比先例更保守的措辞,不是一张不及格判定;先例数字(FEMU、MQSim 6–18%)作为语境写进正文即可。修复另外两点:门槛表放 artifact/协议文件并在 held-out 采集前提交进冻结实验分支(git 提交时间戳即注册证据,E5 矩阵已示范这种诚实标注);rank ≥0.90 的 n 问题按 D2-4 处置(见下)。

**C0-4(微基准不经 GPU 注入路径,近邻工具一行配置可复制)——修复。** 主机侧扫描降格为「器件模型标定」,不当差异化证据卖;在 held-out 集里加一个 GPU 发出的子集:held-out 页数与 random 模式经真实 PTX 改写路径跑(`hbf_vmem_sequential` 用例链路已存在,proof (2) 六断点即此形态),NVMeVirt/FEMU 填任何 profile 都产不出这条,因为它经过改写、覆盖门与页服务。同时正文定位改为:微基准 parity 是入场券,差异化在 E1.3 差分与真实路由(E4)。

**C0-5(模拟侧无变异/无预处理/隔六周漂移)——修复,全收。** (a) parity 比较量定义为「硬件 11 样本的 P50 及其 CI 对模拟点值」,MQSim 合成负载加种子扫描轴;(b) 采集前顺序写满预处理 + 记录 SMART 写入历史进 manifest(照 MQSim/Amber 先例);(c) 重测一个 2026-07-30 标定点作漂移对照——红队自己说一天,照排。合计约 1.5 天。

**D1-1(DRAM 腿断,三介质主轴瓦解)——CONCEDED(DRAM 作保真腿)+ 修复(主轴)。** 承认:116 ns 目标低于 37–51% 插桩地板 [一手,facts L28],逐请求注入在此尺度测出的是开销不是模型误差,修不了。修复主轴:DRAM 点改任「开销地板测量」(即 F1/E2 的对照臂,本来就需要);迁移论证收为两介质(CD8P 必有,CXL SSD 视 Gate 1),并加一句范围论证——HBF 的 tR 是 1–20 µs [一手,brief-1 经事实清单],目标域就是微秒级,亚微秒验证不是 HBF 主张所需。「横跨五个数量级」的卖点撤下。影响:D1 相对 C0 的第一差异点从「三介质」降为「两介质 + 域覆盖论证」,仍强于 C0 的单代理。

**D1-2(链条第 3 环押在 E1.3,失败后全链未定义)——修复。** 前提由 F2 驳倒;退路由 F1+F3 打通;失败分支现在写死:E1.3 失败 → E3 保留但按门槛表降为条件式投影措辞(修订链条第 4 环:E3 的出场资格不再系于 E1.3,只有它的措辞档位系于 E1),E4 降为推算表+轨迹结构事实。章节形态两版都预写(并入盲区 6 修复)。

**D1-3(排队/饱和一格空着,违反自引的 Empirical Guide)——修复。** F4 直接填格:CD8P 硬件 QD 扫描对 MQSim 引擎配对。承认 vmem 曲线档无 QD 旋钮:曲线档的适用范围明写为 QD=1 阻塞语义(这也正是回放线的声明语义,两者自洽);排队保真由 MQSim 档承担并被 F4 锚定。图 2 从「条件图」升为必画图,画的是 F4 那条。

**D1-4(2×3 交叉表混两个适配器)——修复。** 攻击成立。弃用 TinyLlama/llama.cpp 格,嫁接 D2 的 E2-b:同一 vLLM 适配器、同一模型,覆盖取 3 档 × 三模式——驱动变量(建模请求数)被干净隔离,比换模型规模更直接回答 eq2。

**D1-5(CDF 叠画会反噬)——修复。** 攻击成立。曲线档不画 CDF(它是点质量,画了等于自曝无方差模型);CDF 对比只对 MQSim 档画并在图注写明;曲线档改画 P50 parity 加硬件侧 IQR 误差条。图规格改动,零机时。

**盲区 1(引擎缝)——修复**:F5 + F4 + E1.3,见 C0-2。 **盲区 2(MQSim 裁判资格)——修复**:见下 D2-2。 **盲区 3(parity 比较量未定义)——修复**:比较量现在写死——微基准层:模型预测对硬件端到端实测(模型本来就标定为预测含软件路径的端到端量,弱点 5 的口径,两侧同质);应用层:一律差分 ΔT(C0 问题 D),不用 raw 墙钟;`hbfsim_stats_v2` 七类时间域按此分配,作为方法节前置段。半天写作 + 一次 stats v2 验证运行。 **盲区 4(一行配置近邻)——修复**:对内,第 1 天做配置探针(见合并形态 E3);对外,见 C0-4。 **盲区 5(器件状态混杂)——修复**:同 C0-5。 **盲区 6(降级没排练)——修复**:趋势版摘要与贡献列表在第 3–4 天由论文侧(用户/主 agent,不占合作者机时)预写成文,Gate 3 裁决日直接换装;「实测标定」贡献句的趋势版写法:calibrated against, and characterized on, real flash and CXL media(定量档)→ grounded in(趋势档)。

## 二、红队 2(可行性与测量假象)

**C0-1(无天数,不是排期)——CONCEDED(对 C0 单独成立)。** C0 作为排期不可执行,这点修不了也不必修:合并形态用 D1/D2 的表格补上日历,C0 只贡献质量闸门与规范。影响:C0 不能单独胜出。

**C0-2(EQ1 网格机时+开发量超窗)——CONCEDED(全网格)+ 修复(缩格)。** 机时算术部分可商榷(4,800 次 × 30 s ≈ 40 小时,机器夜里跑得完),但 3–5 天 harness 开发 + 稳态预处理是真的。处置:承认全网格死刑,合并形态用缩减矩阵(12 尺寸 × 顺序为主 + 2 模式作特征化 + F4 的 8 档 QD),开发项逐条列名(见排期)。

**C0-3(基线臂被代码挡死、零预算)——修复。** F1 + F6。「未命中永远走 MQSim 计时不可关」由 epsilon profile 化解或经 Snapshot B 一次开关解决;列为开发项、预算 0.5–1 天,不再是零。

**C0-4(EQ3 三输入无代码、预取违禁、机时爆)——CONCEDED(相图)。** 相图三层输入缺两层半,E9 = prohibited 与 §8.5 自相矛盾属实且修不动。合并形态不带相图(D1/D2 已各自砍掉);预取维度本次投稿整体撤下(E9 禁令照守);在线 TTFT/TPOT 撤下,吞吐用离线 tokens/s。影响:C0 §8 整节不进合并形态,EQ3 由 E3(N×tR)+ E4 分开承担。

**C0-5(hybrid 误差语义未定义)——修复。** F5 的三模式同流偏差直接给出 fast 对 reference 的逐流分歧,hybrid 的抽样混合误差随之可归因;措辞采 D2 的收窄口径(只声称成本与旋钮效应)。

**C0-6(冻结与新代码互斥、重跑没算)——修复。** F6。级联重跑的大头被驳掉(分析侧代码不触发重跑);运行时改动合并为一次 Snapshot B;须重跑的颗粒几何矩阵点数:缺,需合作者按 08-16 原始矩阵报。

**D1-1(17.5 人天塞 8 天)——CONCEDED(D1 原排期)。** 逐行相加成立。合并形态给出重排的 8 人天账(见末节),并预声明砍序。D1 的「可行」卖点按原文不成立。

**D1-2(random/strided 是预定失败的验证)——修复。** 这条攻击最有价值,但结论走反了:预定失败的不是测量,是把它标成「held-out 验证」。处置:模式类测量从 held-out 集(过/不过)改入「特征化集」(误差地图,C0 §3.6 自己的概念):照测、照报 −74.8% 量级的误差、写成模型的已知适用边界。配一个负载侧论证:本文负载的未命中流由专家内连续页主导(884,736 次页访问中仅 0.04% 不可预测 [转述,prefetch-accuracy-sweep]),顺序保真是承重维,随机模式误差如实披露且不承重。门槛表只对顺序 held-out 集生效。这不是过度防御——它把一场假验证换成一张诚实的误差地图,且不违反冻结纪律(不改模型)。

**D1-3(DRAM 端墙钟不可验证)——同红1 D1-1,CONCEDED+修复**(已处置)。

**D1-4(零延迟臂堵死、D1 只识别一半风险)——修复。** F1 + F2。红2 自己已指出 backing 机制上可行——采纳,开发项照列。

**D1-5(E4.1 的并发 x 轴测不出来)——CONCEDED(全实测版)+ 修复(降级版)。** 承认:schema 无步对齐信息、契约锁 `max_num_seqs=1` [一手,核查报告 §5],改 schema+重采+过闸 2–4 天不值 E4 的 insight 定位。降级版:在契约允许的单序列模式下多采(约 32 条 prompt 的真实路由轨迹,harness 已有),并发 B 的专家并集用「真实逐序列路由 + 跨序列独立组合」计算,与「独立均匀」零假设对照——比零假设多出真实边缘分布这一层,独立性假设明写在图注。代价约 0.5 天采集 + 论文侧分析。

**D1-6(time_scale 坍缩分支没排)——修复。** F7,两分支预写。

**D2-1(E3-a 主路无代码、备路 QD=1 测不了)——一半驳倒一半修复。** 驳倒「备路测不了目标量」:备路不止回放引擎——`hbf_mqsim_bench` 合成到达 + profile 的 channels/dies/planes 字段(核查报告 EQ3 行 [一手])就是 N 并行读出在开放到达下的稳态带宽实验形态。修复:第 1 天做配置探针,验证 N 与 channels×dies×planes 的语义映射(半天);映射成立则 E3-a 是配置扫描 + 脚本(约 1 天),不是 3 天新代码;不成立则退解析计算 + MQSim 锚点(D1 降级形态)。决断点从第 8 天提前到第 2 天。

**D2-2(E3-b 在 16 KiB 覆盖下退化成查表、Spearman=1 是构造)——CONCEDED(live 版)+ 修复(载体更换)。** 承认:24 次确定性建模启动上换档案,Excel 可精确复算,排序一致是构造结果——live 注入版的 E3-b 在 11 天内不存在。修复:E3-b 换载体,用容量线捕获的真实 MoE 未命中轨迹(每 token 884,736 次页访问量级)在回放引擎 mqsim 档下逐档案重演——缓存命中/逐出/排队交互使结果不可查表,且 harness 已冻结齐备(核查报告总判定第一句)。整节标 trace-driven replay,与 live 数字不混报。E3-c 的排序改在容量矩阵单元(比例×政策×负载)上算,n 升到两位数。

**D2-3(16 KiB 页粒度对 4 KiB 先例,4 倍造假风险)——修复。** `page_bytes` 是 profile 字段;每个命名档案按原文设 4 KiB 并重校验,列为工作项 0.5 天(含受影响回放单元重跑)。不修则图 4 与 262.1 GB/s 手算锚点自相矛盾——照红队说的排进去。

**D2-4(18 人天/压叠)——CONCEDED(D2 原排期)。** 同 D1-1,合并形态重排。

**D2-5(E1-c 时间域不明、无差分)——修复。** E1-c 一律差分:注入臂与 epsilon 臂共享同一插桩路径,差分一阶抵消 37–51% 地板;时间域为墙钟差分,正文写明抵消论证。6787p 的百 ns 级目标随 D1-1 的处置撤出(HBF 域是微秒级)。

**D2-6(并发扫描同坑)——同 D1-5 处置。**

**盲区 1(time_scale,最大假象)——修复**:F7 置于一切 E2 产出之前;缺的 manifest 口径转合作者。 **盲区 2(61 GB 墙当选档论据)——修复**:正文一律标「当前集成的实现限制,不是保真度的机制价格」;选档论据换轨到插桩地板与逐请求同步成本(见 F7 分支二),并注明 FEMU/NVMeVirt 的详细档同样有「细即慢」的定位先例。 **盲区 3(rank 门槛无 n)——见红1 D2-4 处置(下)。** **盲区 4(交接延迟零预算)——修复**:协议加一条「每日产物同步」(tar 包或结果分支推送),排期留 1 天交接/审计浮动。 **盲区 5(快照级联)——修复**:F6,分析侧驳回、运行时侧合并为 Snapshot B。 **盲区 6(归约画图无归属)——修复**:误差/排名脚本、绘图、降级稿、匿名化全部划给论文侧(用户+主 agent),不占合作者与 GPU;第 9–11 天与最后一批运行并行。温度两套数字:动机一节一句话同时给出 −8.10%(热机独占持续负载)与 −3.72%(与 CD8P 并发读)各自的测量条件——一句话成本,cherry-picking 指控消失。此为措辞层,最终由用户过目。

## 三、红队 3(可信度与基准化)

**C0-1(EQ1 证据造不出来)——CONCEDED(C0 原样)。** 与红2 C0-1/C0-2 同根:方向对、基础设施为零、无二轮。合并形态以缩格矩阵 + F1–F6 承接,C0 只作规范供体。

**C0-2(Figure 4 三层全缺)——CONCEDED。** 同红2 C0-4,相图不进合并形态。

**C0-3(OCEAN 同组未刊引用 + 无翻转实验)——修复。** OCEAN 引用整体删除:未刊、同组、双盲不可核,方法论骨架(fidelity-first、时间分解、litmus)从已发表的 Cylon/MQSim/FEMU/Ganger FAST'02 引;CXLMemSim 已发表,第三人称引用合规。翻转实验:采纳盲区 1 的方案(见下)。

**D1-1(DRAM 腿 + 亚微秒可表达性未知)——同前处置**(CONCEDED 保真腿,修复为开销地板 + 域覆盖论证)。亚微秒 profile 可行性问题随主张一起撤出,不再需要回答。

**D1-2(全链收敛在 E1.3、排期太晚)——修复。** 采纳红3 自己的修法:后端切换检查提为第 0 天(F2 使它成为半天验证而非未知数);E1.2 加「只验曲线不验机制」的措辞边界;失败分支预写(同红1 D1-2)。

**D1-3(E2.3 建立在 time_scale 污染数字上)——修复。** F7 提为 E2 的先决闸门,半天成本,红3 自己开的价,照付。

**D1-4(基准化交付物可撤销、「只需新 profile」与核查矛盾)——修复。** 采 D2 的分箱:命名档案套件、冻结轨迹、参考结果 CSV、覆盖 manifest、绘图脚本升为一等交付物,独立于 E3 存亡;承认 held-out 切分与误差计算是要新写的分析代码(约 1–2 天),但按 F6 划归论文侧、离线执行,不占合作者。

**D2-1(E1 撑不起 E3 等高线、裁决他人越权)——修复。** 图 4 三处改:全图标 projection;每条曲线加敏感性带(对未验证的 MQSim 排队参数取合理区间扫出上下界,代替假的误差棒);结论句式从「某论文的结论会翻」改为「若接受某档案的参数,则结论 X 在区间 Y 内成立/不成立」——提供定位坐标系,不下裁决。红3 自己判定此形态可活。E1 与图 4 的传导限度在 limitations 明写:E1 的误差界不延伸到 N×tR 外推区,外推区的可信度由敏感性带与 F4/F5 的引擎锚定间接支撑。

**D2-2(E3-a 退路物理上不存在)——一半驳倒**:同红2 D2-1,`hbf_mqsim_bench` 合成到达是存在的第三条路;决断点提前到第 2 天。E3-b 的 live 卖点损失照 D2-2(红2)承认并换载体。

**D2-3(E3-c 先例错位)——修复。** 同红1 D2-1:E3-c 降格改名为「模式间一致性检查」,不再当验证卖;真值锚定的排序补一条:CD8P held-out 配置上硬件排序对模拟排序(n 小,如实报 n 与精确置换 p 值,不设门槛)。「便宜档保序」的卖点改写为三件套:模式间一致性(实测)+ 引擎锚定(F4/F5)+ 代理域真值排序(实测、小 n 声明)。

**D2-4(删掉了 QD 配对——唯一防 PR#3 类 bug 的形态)——修复。** F4 照单全收,红3 自己说「补一组即可解」。E1-c raw→差分同红2 D2-5。

**盲区 1(无翻转实验,li2026 的 <1% 无人接)——修复,升格采纳。** E4 升为翻转测验:paper/37 独立均匀推算表(批 1 → 6.2% 专家/72.3 tok/s [一手,自标推算])作假设驱动预测,真实路由组合并集(D1-5 降级版)作实测,若在某并发档偏离到足以改变「HBF 在该档可行与否」的判断,即 Accel-Sim §VI 式案例,同时条件式回应 li2026:其 <1% 成立于介质延迟被隐藏的配置,我们给出它不被隐藏的区域(低驻留、高 tR、低 N)。两分支预写:即便实测与零假设吻合,产出也是「均匀假设的适用边界标定」,仍可发表——不赌单一结局。

**盲区 2(验证的路径 ≠ 交付的路径)——修复。** 关键观察:E1.3 差分 A/B 走的正是容量页服务路径(backing 文件),即别人采纳时用的那条线——所以合并形态里 E1.3 直接锚定交付路径,不存在「验 A 交 B」。补一张显式映射表进方法节:哪个实验锚定哪条路径(E1.1→timing 语义;E1.3→capacity 页服务;F4→MQSim 排队;F5→三档缝)。E5 产物交付依赖照红2 盲区 4 的每日同步解决。

**盲区 3(Pareto 两轴不同配置)——修复。** 每个 Pareto 点标注覆盖档;两轴无法同配置时拆成两个子图(成本按覆盖档、误差来自 held-out),图注写明轴的出处,不做假统一。

**盲区 4(标定语料太薄)——修复+承认。** CD8P 尺寸网格加密到 12 点(1/2/4/8/16/32/64/128/256/384/512/768,768 点把「512 外全是外推」的区间实测化,顺带部分化解红1 D2-5)+ F4 的 QD 维——成本是机时不是代码,照红3 开价。承认:单机单盘对先例的四盘,是修不掉的限制,limitations 照写;6787p 若 Gate 1 通过则为第二设备。

**盲区 5(双盲 artifact 零预算)——修复。** 论文侧第 9–10 天排 0.5 天:匿名镜像仓、manifest 去指纹(GPU UUID、`/home/victoryang00/...` 路径)、删 OCEAN 引用、自引第三人称。机械动作,照排。

**红1 D2-5 / 红3 盲区 4 关联的 D2-4(红1)「n=4–6 Spearman 空转」——一半驳倒一半修复。** 驳倒绝对化的那半句:「即使完全一致,常规显著性检验也无从谈起」不对——精确置换检验下 n=6 完全一致的单侧 p = 1/720 ≈ 0.0014,n=5 为 1/120 ≈ 0.0083,统计上有意义;但前提是排序非平凡(全体引擎对 tR 单调时一致是平凡的,红2 D2-2 的告诫并入图注)。修复:一律报 n 与精确置换 p;≥0.90 门槛只对 n≥10 的集合生效(容量矩阵单元提供);小 n 集合只报不设槛。

**红1 D2-5(512 页留出点其实是外推点)——修复。** 采「干净的一半」:模型冻结后新采的离网格点(8/32/128/384/768 + 模式)是唯一承重 held-out;3/3 重切分降为附注性质的稳健性检查并如此标注;512/768 单独报为「外推区实测化」,不并入 held-out 汇总。模型族选择已见过全数据这一点在方法节如实声明(先例中重切分本就常见,声明即可,不冒充事前盲切)。

---

## 四、修复后的最优合并形态 M

**骨架:D1 的四实验链条为主干;E3 换成 D2 的假设重演(载体改为容量回放轨迹);E4 升格为红3 盲区 1 的翻转测验;C0 贡献全部规范件(公平性契约、门槛表、manifest/CSV、差分公式、统计口径);再钉上 F1–F7。**

**E1 机制保真(约 1.1 页,基石)**
- E1.0 第 0 天四闸门:backing 切换脚本验证(F2)、time_scale=1 复测(F7)、`hbf_mqsim_bench` N 映射探针、Gate 1 U787 盘点(并行,不阻塞主线)。
- E1.1 语义与覆盖(已有数字,按字节报覆盖)。
- E1.2 CD8P 加密标定/留出:12 尺寸顺序读为承重 held-out;random/strided 为特征化集(误差地图 + 0.04% 负载论证);漂移对照点;预处理协议 + SMART 入 manifest。
- E1.3 容量路径差分 A/B(CD8P 三臂:epsilon-DRAM / CD8P 实backing 不注延迟 / DRAM+标定注入),ΔT 对比——直接锚定交付路径;6787p 臂视 Gate 1 作第二设备加分项。
- E1.4 QD 配对扫描(F4)——锚定 reference 引擎排队。
- E1.5 缝合(F5)——三档同流偏差 + 截段对 MQSim 原生全栈。
- 图:parity(P50+硬件 IQR,标定空心/留出实心)、QD 带宽与 P99 配对曲线、三臂差分条形。CDF 只对 MQSim 档画。

**E2 保真度的价格(约 0.5 页)**:F7 之后才动笔;载体为同模型覆盖 3 档 × 三模式(D2 E2-b);61 GB 墙标实现限制;插桩地板 37–51% 为机制成本;Pareto 点标覆盖档、轴出处入图注。

**E3 假设档案与 N×tR(约 0.7 页)**:N×tR 走配置扫描(探针决定,备选解析+MQSim 锚点,第 2 天决断);命名档案按原文 4 KiB 页粒度重校验;E3-b 在真实容量未命中轨迹上逐档重演,标 trace-driven replay;E3-c 改名一致性检查,排序统计报 n 与精确 p;图 4 敏感性带 + projection 标注 + 条件式句式。

**E4 路由翻转测验(约 0.4 页)**:真实逐序列路由 + 跨序列独立组合(假设明写)对独立均匀零假设与 paper/37 推算表;两分支预写;条件式回应 li2026。

**合作者 8 执行日账(逐项,超 1 天则按预声明砍序)**:E1.0+Snapshot B 合 1.0;E1.2 采集 1.5;E1.3 差分 1.5;E1.4 配对 1.0;E1.5 缝合 1.0;E2 1.0;E3 1.5;E4 采集 0.5 —— 合计 9.0,超 1.0。**预声明砍序**:① E1.5 的原生 MQSim 半件(−0.5,保三档同流);② E3-b 重演(−0.5,保 N×tR 扫描)→ 落到 8.0。加 3 个日历日:每日产物同步、缓冲、Gate 3 裁决。**论文侧并行(不占 GPU)**:held-out/误差/排名脚本、绘图、趋势版摘要与贡献稿、匿名化、−8.10%/−3.72% 双条件句。

**残余 CONCEDED 的诚实清单(进 limitations,不洗)**:相图与在线 SLO 指标本次没有;预取维度被 E9 禁令排除;DRAM/亚微秒域不在验证范围(HBF 域为微秒级的范围论证随附);live 大覆盖重演没有(replay 标注);并发轴是组合估计不是共调度实测;单机单盘对先例四盘。

**缺项(不可由蓝队补)**:① 20.8x/44.469 s 两侧 time_scale 的原始 manifest——合作者;② 08-16 颗粒几何矩阵重跑点数——合作者;③ Gate 1 盘点结果——合作者;④ 温度线降级触及标题/摘要(D1 差异点 3 原有的 ASK)与 −8.10%/−3.72% 措辞定稿——用户。