# 未决事项清单

> **这一份是同步产物，不是源。** 源文件是 `/root/hbfsim/42-未决事项.md`，改动一律先改源、再复制过来；只改这一份会在下次同步时被覆盖。同步办法：`cp /root/hbfsim/42-未决事项.md /root/hbfsim/HBFSim/docs/39-未决事项清单.md`（复制后把本提示块补回文件开头）。

进这份清单的标准：只有用户能决定、不决定就走不下去。用户拍板或事情做完，当场划掉。
本文件不入 git（`/root/hbfsim/HBFSim/` 才是仓库），所以保留日期。

**2026-08-12 用户定的分工：用户负责写论文，实验由合作者与他那边的 AI 执行。**
因此实验做不做、先做哪个、谁来做，一律不进这份清单，直接写进
`/root/hbfsim/HBFSim/docs/重要实现问题以及需补做实验/15-experiments-we-must-add-before-submission.md`
（按论文贡献点排优先级的工作单）；需要合作者本人判断的问题写进同一目录下的
`16-questions-only-you-can-answer.md`。
这份清单只留写论文本身的决定：主张怎么表述、章节怎么安排、哪条结论敢不敢写。
用户原话：「我主要负责写论文, 不要每次提醒我这个」。

## 一、等用户决定

| # | 卡在哪一步 | 主 agent 的建议 | 详细内容 | 提出时间 |
|---|---|---|---|---|
| ~~1~~ | ~~**温度：补实现，还是收窄 C3 主张**~~ | **2026-08-12 用户裁定：选 (a) 补实现** —— 结温轨迹接进服务速率与数据保持期限，在真实执行中生效成闭环 | `41-合作者设计目标与三条挑战的对照.md` §5.2 | 已决 |
| ~~2~~ | ~~**三条挑战整体重划**（用户：现有 C1/C2「提出的过于失败」）~~ | **已定稿并推送。** C1 逐字按用户原话写；C2 完整落地为「一次访问要花多久无法从这一次访问自己的参数推算」；C3 是规范温度档位下长期能跑到的速率 | 大纲「论文主张」一节 | 已决 |
| ~~9~~ | ~~C1 载重前提存疑~~ | **已证伪并整句重写。** NVBit 不需源码即可插只有二进制的库内核，MICRO 2019 论文量出机器学习负载里预编译库占执行指令 74–96%、平均 88%；设计文档第 32 行的 Non-Goal 是自愿边界不是能力边界 | 大纲 C1、`docs/36-三条挑战的一手核实证据.md` 第三部分 | 已决 |
| ~~12~~ | ~~三条挑战的载重数字一个都不存在~~ | **已转交合作者。** 进了 `16-questions-only-you-can-answer.md` 与 `15-experiments-we-must-add-before-submission.md`；数由合作者那边跑 | 该目录 | 2026-08-13 转交 |
| ~~13~~ | ~~是否实测 NVBit 对驱动 595.84 的适配（README 写 `<= 575.xx`）~~ | **已转交合作者**，是 `16-questions-only-you-can-answer.md` 第 7 条问的第三件事 | 同上 | 2026-08-13 转交 |
| ~~10~~ | ~~三条挑战的呈现顺序~~ | **已定为 C1、C2、C3**，大纲「论文主张」一节按此顺序写 | 大纲 | 已决 |
| ~~15~~ | ~~温度对数据保持时间的影响，以什么形态进论文~~ | **2026-08-12 用户裁定**：移到 discussion／future work，不作贡献主张；大纲写明「是重要问题、目前难做、补上会是很好的贡献点」；列入可选实验清单。温度→服务速率那一半仍作贡献保留 | `46-温度与寿命我们做不做得到.md` | 已决 |
| ~~11~~ | ~~C3 的降级路径未预绑定~~ | **并入 #24。** 评估主线设计文档 47 已给出具体降级形态（选 A 降级为讨论一节；选 B 保留则用静态温度档回退，零实验成本、让 0.15 页），#11 问的「退到哪一档」由 #24 一并裁定 | 文档 47 风险一节 | 并入 #24 |
| ~~14~~ | ~~OCP 规范原文仍未拿到~~ | **已解决。** 用户 2026-08-13 自行下载并交付，现在 `HBFSim/docs/HBF_OCP/`（PDF 4.67 MB 加 .txt 提取，130 页）。**连带纪律：凡引规范一律引英文原件的节号与页码，不引中文转述**——本轮已核出至少四处中文转述里没有的事实 | `HBFSim/docs/HBF_OCP/` | 已决 |
| ~~3~~ | ~~C1 那句答案改成分级处置、C2 改写成测量结论~~ | 并入 #2 整体重划 | `40-C1与D1对照代码核实.md` | 并入 #2 |
| ~~4~~ | ~~「结果逐位一致」没有挑战承接~~ | 并入 #2 | `41-...对照.md` §7 第 1 条 | 并入 #2 |
| ~~5~~ | ~~「模拟器自身开销分开报告」升成 C2 子问题~~ | 并入 #2 | `41-...对照.md` §7 第 2 条 | 并入 #2 |
| ~~6~~ | ~~`intro.tex` 写「8 到 16 倍」、`bg.tex` 写「八倍」，取哪个~~ | **已决，且这一行原来的建议是错的。** 用户 2026-08-13 的原话是「选择表述为至少8倍」（记录在 `56-交接文档.md` 第 274 行与第 348 行），比本行 2026-08-11 的建议晚，所以以那一条为准：我们自己的表述一律写「至少 8 倍」，`8x to 16x` 只在逐字引 OCP 规范原句时保留。2026-09-04 已按此改 `intro.tex`，现在写的是 `The specification puts this tier at least eight times the capacity of HBM at the same read bandwidth` | `56-交接文档.md` 第 274、348 行 | 2026-09-04 已决 |
| ~~16~~ | ~~边缘/笔记本场景进论文哪一档~~ | **问法作废。** 你 2026-08-19 说明「我这是另外一篇论文, 请不要以放进当前sim eval做主要考虑」,所以这不再是「塞进现有论文哪一节」的问题。取而代之的是 ASK.18 | `59-...结论.md` | 2026-08-19 作废 |
| ~~17~~ | ~~是否批准清除无出处的 102.4/4.2/9.2 pJ/bit~~ | **仍待你批准改 HBFSim 仓库**,内容并入 ASK.19 一并处理 | 同上 | 2026-08-19 并入 |
| 18 | **边缘这篇独立论文,论题从省电换成寿命,你认不认。** Amdahl 闸门判定省电主张死透(内存只占笔记本整机 4.9%–8.7%、手机 14.8%–26.4%;换 HBF 后整机功耗是升不是降,笔记本 +5.8% 到 +10.6%);裁判认为这批材料仍够撑一篇独立论文,但论题要换成「端侧限制高带宽闪存的第一约束是寿命,不是能耗也不是带宽」——按规范自己的三条条款(§11.5 每 24–48 小时全量刷新、§9 的 85°C/24 小时锚点、§9.1 结温开到 105°C)与 HeatWatch 实测的 3D NAND 激活能 1.04 eV 折算,同一份常驻数据在 60°C 与 105°C 下的年擦写代价差约 74 倍(29.1 次对 2,168 次),而这是设备什么都不干的情况 | **建议采用寿命论题**,省电那条改写进动机一节正面关掉(「我们先算过能耗,方向是反的,所以约束不在这里」)。不采用的话这批材料只剩容量与手机带宽缺口两条能力主张,野心小得多 | `59-...结论.md` 第〇节第五条 | 2026-08-19 |
| 19 | **两篇论文的贡献边界与双盲怎么处置。** 现有 HBFSim 模拟器论文(FAST '27 秋季轮,2026-09-15 截稿)与这篇边缘论文共享同一套评估基础设施与同一批标定数据。双盲评审下,两篇互相引用要写成第三方口气,而审稿人可能判「贡献边界不清」 | 三种做法:(a) 边缘这篇延后到模拟器论文见刊后再投,边界自然清楚,代价是错过窗口;(b) 两篇同时投不同会场,在各自的相关工作里以第三方口气引另一篇;(c) 把边缘内容作为一节并回模拟器论文,放弃独立成篇。**我倾向 (b)**,但这条只有你能定 | 同上 | 2026-08-19 |
| 20 | **是否批准改 HBFSim 仓库两处。** 一是 `docs/重要实现问题以及需补做实验/15-...md` 第 990–991 行那组无出处的「flash read 102.4 pJ/bit, HBM read 4.2, DDR5-7200 9.2」(自标 primary,两轮检索确认无出处),要换成有出处的区间;二是 `docs/39-未决事项清单.md` 第 55 行写「3D NAND 无公开值,HeatWatch 只给平面 NAND 的 1.1 eV」,要改成 HeatWatch 第 4.2 节实测的 1.04 eV(置信区间 1.01–1.08,R² = 0.76)——这一处是本轮查出的事实错误,项目根目录的 CLAUDE.md 与 42 号清单已改,仓库内那一处还没动(`docs/42-本轮对话全部用户指令-原文.md` 第 374 行也有同样说法,但那份是历史记录,建议不改) | 建议批准。不改的话这两组假前提会继续被下游当成已核实事实 | `59-...结论.md` 第〇节第六条 | 2026-08-19 |
| ~~21~~ | ~~正文 21 页对 12 页上限~~ | **2026-08-21 用户裁定并已执行。** 裁定是页数为硬指标,每节最多超预算 15%、正文整体最多超上限 1.5 页;压缩靠整块删内容、不靠把句子写短。已压到正文约 13.3 页,规则已写进 `~/.claude/skills/systems-paper/SKILL.md` 与本项目 `CLAUDE.md` 第 6 节 | 本文件 | 已决 |
| 23 | **动机一节压到 3.5 页就压不动了,再往下要动实测证据。** 上限是 2 页加 15% = 2.3 页。三轮整块删除之后从 7 页降到 3.5 页,剩下的量集中在三条挑战各自的实测数字与它们的限定条件上——第一条的 23,210 / 12,626 / 10,584 覆盖判定与 110 GiB 容量证据,第二条的六个断点表加 44.469 秒对 2.014352 秒,第三条的 13.1 秒 / 12.4 秒与 379.117 / 348.427 TFLOP/s。**背景一节 2.0 页(上限 1.7)、相关工作 1.4 页(上限 1.15),同样卡在这一层** | 三条路:**(a)** 三条挑战各砍掉一组实测数字,只留最强的那一组,代价是审稿人会问「你凭什么说这是挑战」;**(b)** 把六个断点那张表与容量那半段移进补充材料,正文只留结论句,代价是正文里第二条挑战失去唯一的表格证据;**(c)** 接受正文 13.3 页(在你定的 13.5 容差内),等评估一节写满之后再一次性重排,代价是评估一节还要占 2 到 3 页、届时会冲破容差。**我倾向 (b)** | 本文件;逐节页码见 `HBFSim/paper/main.pdf` | 2026-08-21 |
| 22 | **导师要求「引言简写、用一段话很快地概括」,与大纲给引言的 1.2 页七段冲突。** 原话还原后是「intro 部分可能简写,用用一段话呀,什么很快的概括一下」,出自 `/root/hbfsim/60-会议录音转录校读与摘要.md` 第 4.2 节。这一轮我取的理解是:导师反对的是引言把动机一节重讲一遍,不是要把引言真的压成一段——所以保留七段骨架,只把原来那段 700 词、把三条挑战连同全部证据数字重讲一遍的第五段压成五句话、不带任何证据数字,并指向动机一节 | 如果导师的意思确实是「整个引言只写一段」,那要重写引言,并把现在引言里第一段的容量数字、第三段的三方佐证、第七段的贡献列表分别并进摘要与动机一节。**这一处只有你能确认导师的原意** | `60-会议录音转录校读与摘要.md` 第 4.2 节 | 2026-08-21 |
| 7 | 文档 36 §9 的第 1、4、6、7 条要不要提升进全局 `~/.claude/CLAUDE.md` | 提升第 1、4 两条 | `36-本轮协作中我犯的错误与以后怎么避免.md` §9 | 2026-08-11 |
| 8 | 合作者设计里那几条细节哪些进论文（§8 线程束合并、§10 时间比例、§11 容量约束、§14 错误报告、§3 依赖固定） | 见文档给的分配建议，篇幅紧时先舍 §14 与 §7.3 | `41-...对照.md` §7 第 4 条 | 2026-08-12 |
| 24 | **温度线 C3：从挑战/标题/摘要降为讨论一节，还是保留。** 评估主线红蓝对抗裁定：检出分支温度→时序 0 行实现，11 天内评估章给不出温度闭环实验。选 A 降级——核心贡献句改「实测标定 + 基准平台」，标题/摘要/贡献连带改，评估章无温度实验的矛盾消失；选 B 保留——审稿人必抓「挑战无机制响应」，回退是 E3 档案套件加一个静态温度条件档（用已有热学标定数字，不闭环、不加实验数，E3 让 0.15 页）。原 #11 并入本条 | **三候选与红蓝对抗裁判一致建议 A**；方向性改动，只有你能定 | `HBFSim/docs/47-评估主线设计.md` 风险一节 | 2026-09-04 |
| 25 | **E3 走解析降级时核心主张的措辞。** 若第 2 天探针判明 N×tR 做不成配置扫描，核心主张从「带宽是模拟的输出」改为「按规范组织推导、模拟侧以 MQSim 锚线佐证」——具体句子需要你过目 | 触发才需定；建议按文档 47 E3 降级路径的表述 | 文档 47 E3 一节 | 2026-09-04 |
| 26 | **措辞门槛表是否认可为事前注册政策。** 中位误差 ≤10%→「定量准确」、P95 ≤20%→「有界尾误差」、宏观 ≤15%→「应用级预测」、排名相关 ≥0.90 且 n≥10→「可靠比较基准」；提交进冻结实验分支（git 时间戳即注册证据），论文按表降档措辞、不删实验 | 建议认可；它决定 Gate 3（第 8 天）之后的措辞自由度 | 文档 47 需拍板一节 | 2026-09-04 |
| 27 | **「benchmarking framework」定位句强度两档。** 若 N×tR 探明只是配置扫描、且主机侧微基准形态可被 NVMeVirt 类工具一行配置复制，「别人绕不开我们」需弱化为「用我们的档案套件与参考结果最省事」 | 视第 2 天探针结果选档；两档句子见文档 47 | 文档 47 | 2026-09-04 |
| 28 | **−8.10%（热机独占）/−3.72%（与 CD8P 并发读）双条件一句话写法最终过目。** 动机一节只能有一套热吞吐数字的说法；两次测量的条件差异只有合作者能补（16-questions 第 8 条仍未答） | 句子成稿后过目 | 文档 47；`16-questions-only-you-can-answer.md` #8 | 2026-09-04 |
| 29 | **砍序③的预授权。** 排期超支触发第三级砍序时，E4 的 dense 采集撤下、dense/MoE 收尾收窄为「MoE 在 HBF 上的适用范围」——这触及你「倒数一两个实验证明 dense moe 区别」的逐字约束，要不要预先授权（触发即报，不再等答复） | 建议预授权；不预授权则触发时停下等你 | 文档 47 排期一节 | 2026-09-04 |
| 30 | **Intro 加 Bg 压不到 1.5 页，还差约 0.66 页，砍哪一块由你定。** 2026-09-04 你要求两节合起来在渲染 PDF 上小于 1.5 页。已经压过三轮加一轮审查改稿：从 2.865 页压到 **2.171 页**（量法 `paper/measure-intro-bg.sh`，改稿后的复量见本轮回复）。随后跑了一轮七维度审查加对抗验证（117 条提法，74 条经三票验证成立，清单在 `paper/41-intro与bg审查修改清单.md`），结论是**把清单上每一条都应用完，净只省下 7 个词**——因为省字的合规修改（+189 词）几乎正好被补字的合规修改（−182 词）抵消，而补字的那几条之所以存在，是因为大纲要求的元素或受保护的实测数字现在是缺的，不是因为行文啰嗦 | 四个可选块，按代价从小到大：**(a)** 删 `bg.tex` 的 `tab:tiers` 三方对照表，省 0.17 页——它是结构单元，按本项目协作纪律第 1 条要你先点头；**(b)** 把三条 Observation 的铺垫段整块搬进补充材料，正文只留观察句与各自的反方证据，省约 200 词（0.21 页）——`systems-paper` 允许「移进补充材料并写明是哪一种」，但大纲第 326 行写「各自的例外与反方证据一条都不能省」，所以只能搬支持性材料；**(c)** 再砍 Introduction 约 480 词，这要有人逐段判断哪一段可以整块不要，审查里没有任何一条授权这么大的删除；**(d)** 接受约 2.1 页，把缺口从 Motivation 那边补——Motivation 现在 3.3 页、预算 1 页，是全文最大的超额处。**我倾向 (d) 加 (a)**：Motivation 的超额比这两节严重得多，而 Introduction 现在 0.85 页，已经低于 `systems-paper` 给引言的 1 页下限 | 本文件；`paper/41-intro与bg审查修改清单.md` 的 REMAINING GAP 一节 | 2026-09-04 |
| 31 | **Observation 与 Challenge 的版式我改了，请确认。** `main.tex` 原来把它们排成浅底色加左侧竖线的方框，而大纲第 859 行与第 865 行（依据你 2026-08-13 的原话「challenge, obseravation 在正文时候都要特殊字体注明处理, 参考其他论文如何显目提醒的」）写明「用 `\paragraph{}` 或等效的粗体行内标签……不用带框的盒子」，并记着四篇参考论文没有一篇用方框。我按大纲改成了粗体行内标签加斜体句子，`\obsref{}` 与 `\chref{}` 的全称回指不变 | 建议保留改动。它同时影响 Motivation 一节的三条 Challenge——这是大纲要求的方向，但确实动到了你这一轮没点名的一节，所以在这里报备。要退回方框只需还原 `main.tex` 的 `\hbfcallout` 一个宏 | `paper/main.tex` 的 `\hbfcallout`；大纲第 859、865 行 | 2026-09-04 |
| 32 | **arXiv 自动提交要不要走 SWORD 授权申请。** arXiv 确实有一个投稿接口（SWORD/APP Deposit API，HTTP Basic 认证，端点 `https://arxiv.org/sword-app/servicedocument`），但它自己标着 beta，而且原文写明 `prospective users of the deposit API must request authorization to do so via email to arXiv administrators`——账号密码不够，要先发邮件申请开通 | 两件事请你定:一,要不要现在去发这封申请邮件;二,授权办下来之后凭据怎么给。**我建议凭据不要交给主 agent**——终端会留下命令记录,而且跨会话不保存、每次都要重给;更稳的是授权到手后由你自己在终端用 `!` 前缀跑那条 `curl`。压缩包已经打好在 `paper/arxiv/hbfsim-arxiv.zip`,随时可以手工上传 | `63-arXiv自动提交能不能做与提交包.md` | 2026-09-04 |
| 33 | **对照表要不要加带宽这一行,加的话用哪一套带宽数。** 大纲第 279 行把带宽列为这张三方对照表必须有的六项之一,而 `bg.tex` 的 `tab:tiers` 现在没有带宽行。卡住的是取值:大纲第 279 行与 `motv.tex` 第 270 行写「规范三档约 0.4 到 3.0 TB/s」,而一手核实文档 `HBFSim/docs/重要实现问题以及需补做实验/18-spec-conformance-findings.md` 第 186 至 194 行按规范 Table 4 给的是 0.384 / 1.536 / 3.072 TB/s,并指出规范自己相隔两页处的单位对不上 | 两条路:**(a)** 按 18 号核实文档写 0.384 / 1.536 / 3.072 TB/s,并在表注里写明规范自己的单位不一致——这是一手读数,我倾向这条;**(b)** 沿用大纲的 0.4 到 3.0 TB/s,代价是审稿人对着规范一查就能发现取整口径不明。加这一行会让表变高、正文再多占约 0.02 页 | 审查修改清单 `paper/41-intro与bg审查修改清单.md` 第 35 条 | 2026-09-04 |
| 34 | **引言第一段要不要给一个具体规模的数字。** 大纲第 257 行要求第一段给一个具体规模的数字(KV cache 有多大),而 `/root/hbfsim/tmp/ref-paper-引言逐段拆解.md` 第 427 行从三篇同会场范文里量出来的第一段规格是「0 个阿拉伯数字、最多两处不带数字的量级说法」。两条都是我们自己写下的要求,现在的第一段按后者写,没有数字 | 两条路:**(a)** 保持现状,不给数字,第一段只给量级说法——与 Cylon 的第一段一致(它整段 0 个数字),我倾向这条;**(b)** 加一句不带算式的结果数,例如「已经超过一堆 HBM 装得下的量」加一个具体值,代价是范文规格被破坏、而且这个数在 Background 会再出现一次 | 审查修改清单第 36 条 | 2026-09-04 |
| 35 | **`related.tex` 里 MQSim 的交叉引用指错了地方。** 审查发现 `related.tex` 把「我们只取 MQSim 的闪存介质模型、在线提交请求而不是事后回放」这件事的出处指向 Background,而 Background 现在没有这一段(大纲背景 P7 计划里有,正文里没写)。这一轮的范围是 Intro 与 Bg,所以我没有动 `related.tex` | 两条路:**(a)** 改 `related.tex` 的交叉引用,指向 Implementation 一节(那里确实讲了复用哪些部件);**(b)** 把大纲背景 P7 那一段补回 Background,代价是再多占约 0.06 页,而这两节已经超预算。**我倾向 (a)** | 审查修改清单第 23 条 | 2026-09-04 |
| 36 | **两条贡献的加粗标题与正文对不上了,标题怎么改由你定。** 审查清单第 17 条按大纲的裁定把贡献一的正文收窄到只claim「温度改变服务速率」这一半,数据保持规则与强制 refresh 流量那一半移进讨论一节;第 12 条把贡献三正文里的超显存那半句删掉(它在第六段与结果预览里各出现一次)。两条的加粗标题没有跟着改:贡献一仍写 `Temperature and reliability taken from the specification into a running workload.`,而正文里现在没有 reliability 那一半的claim;贡献三仍写 `... and a capacity mode.`,而正文里现在没有容量 | 两条路:**(a)** 把标题跟着正文改窄,例如贡献一改成只提温度与服务速率、贡献三只提逐字节一致的注入——代价是「容量」这个能力在贡献清单里不再露面;**(b)** 把正文补回去,代价是页数再多约 30 词,而这两节已经超预算。**这一处与 ASK.24(温度线 C3 降不降级)是同一个决定的两面**,建议与 #24 一起定 | `paper/41-intro与bg审查修改清单.md` 第 12、17 条 | 2026-09-04 |
| 36 | **容量模式在论文里怎么定位——本轮核实查出的结构性问题。** 核实 OCP 规范与五篇已发表 HBF 工作后确认:业界主流是「HBM 与 HBF 两层并列、按数据类型分工、加速器直接读 HBF」,延迟靠基底裸片上 8–40 MB 的 SRAM 掩盖(H³ 按 `2×带宽×延迟` 算得 40 MB);**没有一篇把 HBM 当 HBF 的缓存**,规范 §11.1.2 还写明两者一起用时必须分开管理、§13.2.1 把「先搬进显存」当旧做法否掉。而 HBFSim 容量模式是 2 GiB 显存缓存配 110 GiB 逻辑范围(约 1:55)的页缓存结构,已发表比例是 1:8 到 1:16;审稿人会打三处:与我们自己引的规范条文冲突、1:55 无先例、量到的减速里多出缺页与逐出这一项(已发表设计里这一项为零) | 三选一:**(a)** 按模式分账——时序模式(数据留在显存、只注入延迟)本就是直读结构,让它承担全部器件主张,容量模式如实写成「今天没有 HBF 器件、为让超显存负载跑起来的模拟手段」,代价是文档 47 的 E1.3 改写成机制验证、E3 驻留比例线重挂时序模式;**(b)** 保留搬运路径但把缓冲区按 `2×带宽×延迟` 缩到几十 MB 重新锚定,代价是改代码尺寸并重跑容量证据;**(c)** 两个都做、报差异,多约 1 人天,收益是把最大质疑变成「唯一量出搬运假设代价」的贡献。**我建议 (a) 加 (c)** | 本轮核实报告;文档 47 待改 | 2026-09-04 |
| ~~37~~ | ~~两份讲义的作者归属错了~~ | **前提不成立,本条作废——错在主 agent 自己。** 亲手核对 `docs/ref_article/README.md` 第 715–717 行,登记写的就是 `Joungho Kim (김정호), Professor, School of Electrical Engineering, KAIST`,从来没写成 SK hynix;是主 agent 在给核实代理的交接说明里把这两份讲义说成 SK hynix 演讲,代理指出了这个错误,主 agent 却把它记成了 README 的错误。**实际可做的两处**:一,README 第 720 行写研讨会日期 2026-02-10(与 PDF 创建时间相符),而讲义封面印的是 `2026.2. 3`,建议补一行注明封面日期;二,金正浩 2025-09-11 那份 136 页讲义(印有 HBM 100 GB / HBF 1 TB,是那组数字的一手出处)本地没有,已下载到临时目录,要不要收进 `ref_article/` 并登记,请你定 | 主 agent 已补第一处;第二处等你发话 | 2026-09-04 作废并更正 |

## 一点五、已裁定、等工作流结束后执行

| # | 裁定 | 执行内容 | 时间 |
|---|---|---|---|
| K1 | **C1 不再用「主机侧」这个词** | 规范里的 `Host` 指 xPU，与我们说的服务器 CPU 相反，同一个词指两个相反的东西。改成「服务器 CPU 那一侧」或直接写「被测程序之外」。同时把「效应只能写进被测程序自己的**指令流**」改成「写进被测程序自己的**代码，包括它的运行时**」——因为写路径很可能由运行时软件维护，不是一条存储指令 | 2026-08-13 |
| K2 | **写路径的层次错位升级为给合作者的头号问题** | 规范给写的约束是 4 KiB 粒度与对齐、非投递写、块内必须连续（违反错误码 `0x6`）、写块首页自动擦除整块、未在超时内凑齐 4 KiB 返回错误码 `0x5`。**没有任何 GPU 指令集的存储指令有这样的语义**，所以写几乎不可能是普通 `st.global`，只能由一段软件维护。**后果：我们改写 `st.global` 来建模写，可能建模的是真机上不存在的路径**，影响最大的是键值缓存这个写密集场景与容量模式的脏页写回。读那一侧（64 字节粒度、突发不跨 4 KiB 页边界）不受影响。**只写进给合作者的文档，论文与大纲不提** | 2026-08-13 |

## 二、等主 agent 做（用户拍板后执行）

| # | 事项 | 依赖 | 提出时间 |
|---|---|---|---|
| A | 按裁定改大纲 C1／D1、C2／D2 四段 | 待 #2 #3 | 2026-08-12 |
| B | 修订 `39-两条挑战改前改后对照.md` 第 22 行词表末行与第 68 行那句答案 | 待 #2 | 2026-08-12 |
| C | 大纲 C1 补两句：判断对象是内核启动、那 10,584 次是被放行的 | 待 #2 | 2026-08-12 |
| D | 快慢两条路径一致性检查：代码与测试里都没有，大纲第 130 行已许诺 → 进 `docs/EXPERIMENTS-NEEDED.md` | 无 | 2026-08-12 |
| J | **拆 FlashAccel 第 5 节 Data Layout Design（第 5–9 页，约四页）**：那是他们论文的主体，也是我们唯一可能正面相撞的地方；现在 `46-...做不做得到.md` 只写了页数没写内容，写相关工作前必须补 | 无，确定要做 | 2026-08-12 |
| I | **改大纲第 34 行的事实错误**（已核实）：CXLMemSim 不是「覆盖天然完整」，原文自述 `it's always a sampled model which may lose a lot of data`、`does not require full-cycle simulation of every load/store`；Cylon 那半成立（`Every guest access undergoes address translation through Intel's EPT`）。这是合作者自己的前作，不能写错 | 无，确定要改 | 2026-08-12 |
| E | 大纲 8 处 `[todo]`：主机 CPU／内存／系统／内核／GCC 一次性采集，加 CXL-SSD 堆叠平台参数 | 无 | 2026-08-11 |
| G | **温度闭环实现**（用户已选 (a)）：结温轨迹 → 服务速率、结温轨迹 → 数据保持期限，两条都要在真实执行中生效；现状是只有一份实测拟合 `configs/thermal/gpu-cd8p-logp-live.json` 与 27 行拟合测试，无任何代码把温度接回时序或可靠性 | 待 #2 定挑战表述后写进 `docs/EXPERIMENTS-NEEDED.md` 交合作者 | 2026-08-12 |
| H | 温度闭环的验证实验：热起来之后服务速率的变化要能被量到并与实测拟合对上；Ea 取 HeatWatch 第 4.2 节实测的 3D NAND 值 1.04 eV（置信区间 1.01–1.08，R² = 0.76），并按置信区间两端做敏感性扫描（2026-08-19 更正：此前记的「3D NAND 无公开值」只读了该文背景一节，同一篇后面自己测了） | 待 G | 2026-08-12 |
| L | **补 40 处缺的参考文献条目。** 这一轮六节正文里留了 40 个 `\TODO{}`,绝大多数是「这一篇还没进 `refs.bib`」。缺的是:OCP HBF 规范 v0.7.0、SanDisk HBF Fact Sheet、SK hynix 的 H³、Kimi K3 模型卡、Llama 3.1(arXiv:2407.21783)、GPGPU-Sim、MGPUSim、gem5、SimpleSSD、QEMU、Qwen3、vLLM、MaxMem(arXiv:2312.00647)、XNACK/MI300A(arXiv:2508.12743)、DiskSim 手册(CMU-PDL-08-101)、JEDEC JESD218B、Cai 等 Flash Correct-and-Refresh(ICCD 2012)、Pre-gated MoE(arXiv:2308.12066)、Kouchi 等(JSSC 2021)、NVIDIA H100 数据手册与机架参考架构、铠侠 GP1。**本轮已按核实纪律加进去 13 条**(MQSim、FEMU、Accel-Sim、HBF Sucks、Micron HotInfra'26、屋顶线综述、Pope 等、Hydragen、HAVEN、Liu FAST '12、Luo 等 3D NAND、Li 等 HotStorage '21、Fusco 等、SwarmIO) | 无 | 2026-08-21 |
| M | **`docs/ref_article/README.md` 里有十份文件没有登记条目**,其中六份正是这一轮背景一节第三条观察的全部证据(`liu2012`、`li2021`、`jeong2014`、`cho2024`、JEDEC、`luo2018-3d`),另外四份是第一条观察用的(`yuan2024`、`pope2023`、`juravsky2024`、`kim2024-duplex`)。本轮已核出后四份的 arXiv 编号、标题、第一作者与提交日期,还缺 SHA-256 与页数 | 无 | 2026-08-21 |
| N | **两个文件名与命名规则不符,要改名:** `kim2024-duplex-moe-attention-accelerator` 的第一作者是 Yun, Sungmin(首尔大学),应为 `yun2024-`;`pope2023-efficiently-scaling-transformer-inference` 的 arXiv 提交年是 2022,`refs.bib` 里已按 `pope2022scaling` 建键,文件名与键不一致。**这两处要动合作者的仓库,先问你** | 待你批准动仓库 | 2026-08-21 |
| O | **`eval.tex` 现在没有报结论一节引用的那三个数**(六个标定断点全部对齐且 0 次不安全启动、快速路径快 20.8 倍、110 GiB 加 2 GiB 页缓存下 checksum 与基线相同),所以结论与评估两节现在对不上。评估一节这一轮不在范围内,记在这里 | 待评估一节动笔 | 2026-08-21 |
| P | **两处只能人工在浏览器里取的资料。** 一是 SK hynix 自己写的 H³ 摘要页(`https://research.skhynix.com/blog/detail/summary-h3-hybrid-architecture-...`),**不需要订阅、只是挡自动化访问**,取到就能把「读延迟 20 µs 是 H³ 的输入假设」这一条从中文转述换成接近一手的英文来源;二是 HotStorage '21 那篇在 ACM 的条目页(`https://dl.acm.org/doi/10.1145/3465332.3470877`)返回 403 人机校验,该文的 DOI 与页码现在是从检索结果转述的、没核死 | 待你代取 | 2026-08-21 |
| Q | **新查到一篇很近的工作,要判断撞不撞车:** SwarmIO(`arXiv:2604.06668`,2026-04-08,Hyeseong Kim、Gwangoo Yeo、Minsoo Rhu),`Towards 100 Million IOPS SSD Emulation for Next-generation GPU-centric Storage Systems`。本轮只读了摘要就写进相关工作一节:它自陈的第三个困难 `excessive timing-model maintenance overhead at extremely high I/O request rates` 与我们第二条挑战是同一类冲突,而它的拦截点(NVMe 提交队列、门铃寄存器、可替换驱动层)正是 HBF 上不存在的那个。**要读全文才能定它与我们第二条挑战的边界,以及对照表里 SwarmIO 那一行每一格的取值**(现在「GPU 侧」与「实测标定」两格都写的是「未确立」) | 无 | 2026-08-21 |
| R | **用户裁定 #24 并批准文档 47 主线后的两处同步：** 大纲 35 评估一节从 14 问改写成 E1–E4 四实验结构（页数账跟着改）；15 号工作单 23 项按文档 47 附录 C 的映射重排优先级说明 | 待 #24 与文档 47 批准 | 2026-09-04 |
| S | **四条先例引用在写进正文前按 verify-paper-sources 核原文：** Ganger 等 Timing-Accurate Storage Emulation（FAST '02，方法节要引、现只有 19 号规划的登记）；CXL-DMSim（TCAD，未亲读）；Sieve 的会场信息（正文页眉未给出）；CXLMemSim 的 HPDC '26 发表版条目（brief-2 读的是 arXiv v2）。OCEAN 已裁定不引用（未刊 + 同组 + 双盲不可核） | 待写相关工作/方法节 | 2026-09-04 |
| ~~F~~ | ~~评估设计文档推送~~ | **已执行(2026-09-04)。** 用户选定 47C 为正式版并指示推送;分支 `docs/eval-mainline`,提交 `c64a62b`:文档 47 + 辅助材料子目录 18 份 + 16-questions 第 9 节。**paper 子模块(大纲 35 的 2 页预算行等)仍未推**,待用户对子模块改动发话 | paper 子模块推送仍待发话 | 2026-09-04 完成 |
