%-------------------------------------------------------------------------------
\section{Background}
\label{sec:bg}
%-------------------------------------------------------------------------------
% Keep short; fold into Motivation if space is tight.
%
% Only what a reader needs to follow the design:
%   * the proposed flash-backed near-compute tier and its published parameters
%   * how GPU kernels reach memory (PTX, cubin, and what that implies for
%     anything that wants to observe or change an access)
%   * the flash media model we reuse, and what we deliberately bypass
%   * thermal behaviour of the devices under sustained load
\TODO{background}
