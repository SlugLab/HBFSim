Root latest decision: worker12600/outer13200/collection600/startup300/full14100, after user explicitly allowed considering longer runtime. Prior10800 plan preserved as superseded; no LIVE_PLAN existed at change. Primarylease06:30UTC must cover fullwindow atlaunch.

# Final147 budget frozen for root preparation

DOC_DERIVED: remaining45 epoch6711 passed MODEL_CONNECTED_PASS_COMBINED_REMAINING45; controller elapsed2180.826370213s, clean. Root chose epoch6712 worker10800/outer11400/collection600/startup300/full12300 seconds. Conditional old6603 controller2457.664 + remaining45 controller2180.826 + head candidate3314.621 + mixed3 selected91.103 =8044.214s; setup is deliberately double-counted, and ~34% worker margin is not a guarantee. The template is ready for root identity/resource/lease checks and exact LIVE_PLAN review. No final147 START or PASS is asserted. Request timeout remains780s per-service wait, unchanged model/profile/helper/sampling. Earlier unset-budget wording below is historical.

# Final 147 single-run regression — isolated CPU template

Status: **CPU scaffold only; remaining45 result and finite budget pending. No LIVE_PLAN, GPU launch, resource action, or final model PASS.** The active mixed3 packet and canonical combined plugin/host/gate/staged PTX/validator/controller/guard are unchanged. This separate launcher writes only a future `runs/li6-li7-final147-v1` output and requires an exact root review receipt before launch.

Scope is one real model run, not a union of historical runs. The command registers the exact 147 distinct storages and 13,838,323,712 B from `task/full-coverage-ledger-v1/TARGET_LEDGER.json`. Its five anchored include patterns comprise the three accepted old98 patterns from `task/qkv-model-combined-v1/config-final-v2/runtime-config.pre-resource-combined.json:scopes.all98.include_patterns`, plus one pattern for layers0–15 QKV/o_proj/router and one for `lm_head.weight`. A CPU set check matched all 147 ledger aliases, no extras among the ledger, and the exact byte total. The combined plugin selects its exact `prepared-v2/scope-added49.json` 49 targets while the old98 are also registered and must have active address/service closure in the same run. Head is part of this final run after the model layers; earlier head representative PASS cannot substitute for the final combined callback/lifecycle proof.

The canonical `validate_combined_model.py` already supports scope `added49`: it demands full147 registration equality to TARGET_LEDGER, 49 selected native/candidate raw outputs and model/request/thread/storage/Driver joins, old98 modeled address rows in active closed modules, two selected exact module closures, final tokens, target image/map identity, and successful bounded controller. Its source/criterion are reused unchanged and a future invocation supplies the final `--run`/`--plan`. No per-storage latency theorem is added; no old result is relabeled as this run.

Prepare and launch both require actual `MODEL_CONNECTED_PASS_COMBINED_REMAINING45`, `MODEL_CONNECTED_PASS_COMBINED_MIXED3`, and `MODEL_CONNECTED_PASS_LM_HEAD` receipts with pinned paths/hashes/epochs. The template budget fields are zero, deliberately making prepare fail until root uses measured mixed3 and remaining45 costs to choose one finite worker/outer/collection/startup/full window and an adequately long actual reservation self-deadline. The request timeout is 780 s, the maximum prior accepted head per-request service-wait bound from epoch6706; it is not the aggregate candidate duration or a predicted final ETA. Model input, sampling, helper, PTX, profile, and output criterion remain unchanged. The old 6603 controller duration 2457.664 s and head6706 candidate3314.621 s show why a short mixed pilot is not a sufficient full147 budget, but they do not determine the final sum.

The launcher reuses configured selected-GPU checks, current boot/PID/start and fresh finite START/renewal receipt joins, exact configured→resolved DSO maps, actual target occupancy capture, bounded controller/guard, and shared owner lock. It cannot prepare from stale active mixed3 resource identities. Root must re-review this isolated script and exact filled template before any GPU action. No new helper/host ABI or scientific input is proposed.
