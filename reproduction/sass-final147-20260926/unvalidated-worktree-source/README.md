# Separately preserved research worktree sources

These23 formerly untracked source/configuration/doc files complement `../CURRENT_RESEARCH_WIP_FROM_EABC.patch`. They preserve the local research checkout for future development. They are not overlaid on the accepted final147 runtime, and their historical preflight/status text does not grant a new execution authorization or supersede the final report. Build directories and raw output are excluded. No new numerical or GPU validation of this WIP was performed.

The original `experiments/rebuttal_20260921/R1_MANIFEST.json` is malformed JSON (line57). Its exact bytes are preserved as `R1_MANIFEST.json.invalid-original`; the inventory maps the original name. It is not a runnable preflight manifest, has not been silently repaired, and is excluded from executable configuration. The first publication check that found it is retained in `../verification/PUBLICATION_CHECK_INITIAL.json`.
