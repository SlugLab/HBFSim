# Final 147-storage SASS/model integration evidence and source

This packet records the accepted **fixed model request**, not universal kernel, model, or consumer coverage. The final single run registered 147 unique storages, joined modeled address evidence and completed module service, compared 49 selected native/candidate outputs, and returned tokens `[70, 13]`. Historical union evidence is separately labeled.

Start with [INTERFACES.md](INTERFACES.md), [REBUILD.md](REBUILD.md), and [PUBLICATION_FILE_MANIFEST.json](PUBLICATION_FILE_MANIFEST.json). `evidence-tree/` preserves original workspace-relative provenance; absolute paths in historical receipts are observations, not portable defaults. The final report and validator are retained there. See `upstream-review/` for separately reviewed upstream PRs; those findings do not rewrite the historical fixed-request PASS.

Publication CPU plugin fixtures passed (see [the verification receipt](verification/PUBLISHED_PLUGIN_CPU.json)). A clean source rebuild and cross-machine GPU reproduction were **NOT performed** for this publication. No model weights, CUDA/vendor images, generated PTX, shared libraries, archive caches, or large raw traces are redistributed. Re-obtain licensed inputs, rebuild binaries, and produce new runtime identities before executing any launcher. Historical launch scripts and plans are evidence, not ready-to-run commands on another machine.

`first-fault-runtime-from-eabc5c2.patch` reconstructs the accepted helper/runtime source overlay. `CURRENT_RESEARCH_WIP_FROM_EABC.patch` instead preserves the current dirty research checkout and is **unvalidated WIP**, not applied to accepted runtime or main production code. Both use HBFSim eabc5c2 as their base. The agent patch uses a different repository: bpftime ec26daec.

The [unvalidated worktree source inventory](unvalidated-worktree-source/INVENTORY.json) preserves accompanying source/configuration files separately from accepted components.

The [upstream repair and merge follow-up](upstream-repair-followup/README.md) records the later five merged fixes, 107 CPU checks, and the HBFSim coverage-gap analysis.
