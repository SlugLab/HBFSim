// Keep the live thermal proof on the same PTX future operations as the
// bit-exact SM120 correctness gate.  The host runner executes this image under
// declared Normal and Light temperature profiles and compares every result.
#include "sm120_future_correctness.cu"
